# ──────────────────────────────────────────────────────────────────────────────
# requirements.txt — Compatible Python 3.14 + Render.com (Avril 2026)
# ──────────────────────────────────────────────────────────────────────────────

Flask==3.1.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-JWT-Extended==4.7.1
Flask-CORS==5.0.0
flasgger==0.9.7.1
PyYAML==6.0.2
python-dotenv==1.0.1
bcrypt==4.2.1
gunicorn==23.0.0

# === CORRECTION PRINCIPALE ===
# psycopg[binary]==3.2.3  →  cette version exacte du binaire n'existe pas pour Python 3.14
# Utiliser une version récente sans pin trop strict
psycopg[binary]>=3.2.0

# Option plus souple (recommandée pour Render) :
#psycopg[binary]<3.3